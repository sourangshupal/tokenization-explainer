# Pretrained models

`sp_unigram_tiny.model` was trained on `data/tiny_corpus.txt` with SentencePiece
Unigram (`vocab_size=100`). Use it only when `sentencepiece` install/training fails
on a student machine — see notebook 03.
